# WAIO Controller default pipeline
# ordered list of workers/registry.conf NAMEs to execute in sequence when
# ORCHESTRATE (workers/orchestrate_worker.sh) runs with no override.
# lines starting with # and blank lines are ignored.
# each NAME here MUST already exist in workers/registry.conf -- this file
# never introduces a worker/agent itself, it only orders existing ones.
# do not list ORCHESTRATE here (it would recurse into itself).
RESEARCH
ANALYSIS
AI
